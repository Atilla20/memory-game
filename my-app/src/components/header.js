import React from 'react';

const Header = () => {
    return (
    <header>
        <h1> Clicky Game!</h1>
        <div> Click on an image to earn points, but don't click on any more than once!</div>
    </header>
    )
}

export default Header;