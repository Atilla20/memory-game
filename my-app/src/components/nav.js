import React from 'react';


let score = 0;
    for(let i = 0; i < score.length; i++);


let topScore = 0;
    for(let j = 0; j < topScore.length; j++);



const Nav = () => {
    return (
    <nav> 
        <h2>Clicky Game</h2>
        <h2> Score: {score} | </h2>
        <h2>Top Score: {topScore}</h2>
    </nav>
    )
}

export default Nav;